# Frontend (Vite) — só chaves públicas
# Para rodar local: `supabase status --output env` e copiar VITE_SUPABASE_URL + ANON_KEY
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
NODE_VERSION=20
# Chaves server-side ficam em supabase/functions/.env.local (ver .env.example lá)
